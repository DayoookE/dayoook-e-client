version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g npm@9.8.1
  pre_build:
    commands:
      - npm install --legacy-peer-deps
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
