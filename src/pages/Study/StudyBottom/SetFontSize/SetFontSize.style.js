import styled from 'styled-components'

export const FontSizeWrapper = styled.div`
  background: linear-gradient(
    145deg,
    rgba(255, 142, 158, 0.5),
    rgba(255, 142, 158, 1)
  );

  border-radius: 10em;
  height: 1.5em;
  margin: 1.5em 0 0.5em 0;
`
